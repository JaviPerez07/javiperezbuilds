{
  "name": "javiperezbuilds-static",
  "private": true,
  "scripts": {
    "build": "node scripts/validate-static.mjs",
    "lint": "node --check scripts/config.js && node --check scripts/operator-atelier.js && node --check tests/production-visibility.mjs",
    "test": "node tests/production-visibility.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
