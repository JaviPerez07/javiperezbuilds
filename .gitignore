# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local dev artifacts (not needed in production)
serve.py
.claude/

# Logs
*.log

# Node (none used at runtime, but keep clean if anyone runs lighthouse locally)
node_modules/
npm-debug.log*

# Variables locales. Solo se versiona el ejemplo público.
.env
.env.*
!.env.example

# Scratch local (no publicar)
__scroll-local.html

# Copy retirada de la home (guardada para referencia, nunca se despliega)
archive/
